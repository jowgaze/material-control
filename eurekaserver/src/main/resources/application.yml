spring:
  application:
    name: eurekaserver
  security:
    user:
      name: ${EUREKA_NAME:eureka}
      password: ${EUREKA_PASSWORD:eureka}

server:
  port: 8761
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false